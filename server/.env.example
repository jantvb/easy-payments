# Stripe TEST MODE only — never commit a real .env with credentials.
#
# Copy to server/.env and replace with your Dashboard TEST secret key:
#   STRIPE_SECRET_KEY=sk_test_...
#
# Angular uses pk_test_... only (never sk_...).

STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PORT=3000
FRONTEND_ORIGIN=http://localhost:4200
